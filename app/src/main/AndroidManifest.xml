<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.karan.suman.kiitquestionbankforadmin">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name="android.support.multidex.MultiDexApplication"

        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">


        </activity>

        <activity android:name=".RegisterNewUserActivity">



        </activity>
        <activity android:name=".BranchAdminLoginActivity">

        </activity>
        <activity android:name=".MainAdminLoginActivity" />
        <activity android:name=".QuestionUploadForAdminActivity">
            </activity>
        <activity android:name=".AdminAfterLoginActivity" />
        <activity android:name=".AddBranchActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>