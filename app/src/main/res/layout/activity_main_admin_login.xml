<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="in.karan.suman.kiitquestionbankforadmin.MainAdminLoginActivity">

    <EditText
        android:id="@+id/email"
        android:textColor="#009f3d"
        android:layout_width="350dp"
        android:textColorHighlight="#009f3d"
        android:shadowColor="#009f3d"

        android:layout_height="70dp"
        android:layout_alignParentTop="true"
        android:onClick="#009f3d"
        android:hint="Email..."
        android:layout_centerHorizontal="true"
        android:layout_marginTop="108dp"
        android:ems="10"
        android:inputType="textPersonName" />

    <EditText
        android:id="@+id/password"
        android:onClick="#009f3d"
        android:layout_height="70dp"
        android:textColor="#009f3d"
        android:layout_width="350dp"
        android:textColorHighlight="#009f3d"
        android:shadowColor="#009f3d"

        android:hint="password"
        android:ems="10"
        android:inputType="textPassword"
        android:layout_centerVertical="true"
        android:layout_alignStart="@+id/email" />

    <Button
        android:id="@+id/login"
        android:layout_width="200dp"
        android:layout_height="70dp"
        android:text="LOG IN"
        android:textColor="#ffffff"
        android:textStyle="bold"
        android:textSize="20dp"
        android:backgroundTint="#37B05C"
        android:layout_marginTop="38dp"
        android:layout_below="@+id/password"
        android:layout_centerHorizontal="true" />



</RelativeLayout>
